// src/diagrams/matrix/tokenBuilder.ts
import { AbstractMermaidTokenBuilder } from '../common/index.js';

export class MatrixTokenBuilder extends AbstractMermaidTokenBuilder {
  public constructor() {
    super(['Matrix']);
  }
}
