// src/diagrams/array/tokenBuilder.ts
import { AbstractMermaidTokenBuilder } from '../common/index.js';

export class ArrayTokenBuilder extends AbstractMermaidTokenBuilder {
  public constructor() {
    super(['Array']);
  }
}
