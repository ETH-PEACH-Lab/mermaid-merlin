name: Draft Release
# DISABLED: Changed to workflow_dispatch to disable automatic runs
on:
  workflow_dispatch:
    inputs:
      force_run:
        description: 'Force run release drafter (disabled by default)'
        required: false
        default: 'false'
  # push:
  #   branches:
  #     - master

permissions:
  contents: read

jobs:
  draft-release:
    runs-on: ubuntu-latest
    permissions:
      contents: write # write permission is required to create a GitHub release
      pull-requests: read # required to read PR titles/labels
    steps:
      # DISABLED: Commented out to prevent pr-labeler.yml configuration errors
      # - name: Draft Release
      #   uses: release-drafter/release-drafter@v6
      #   with:
      #     disable-autolabeler: true
      #   env:
      #     GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - name: Placeholder step
        run: echo "Release drafter is disabled"
